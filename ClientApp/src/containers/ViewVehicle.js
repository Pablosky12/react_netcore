import React, {useEffect} from 'react';
import axios from 'axios';
const baseUrl = "https://localhost:5001/api";

const ViewVehicle = (props) => {
    console.log(props);
    // useEffect(()=> {
    //     axios.get(baseUrl+'/vehicle/')
    // })
    <div>
        <legend>
            Vehicle {vehicle.id}
        </legend>
    </div>
}